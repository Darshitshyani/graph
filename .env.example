# Shopify App Configuration
# These variables are automatically provided by Shopify CLI during development
# For production, set these in your hosting platform's environment variables

# Required: Shopify App API Key
# Get this from your Shopify Partners dashboard under App credentials
SHOPIFY_API_KEY=your_api_key_here

# Required: Shopify App API Secret Key
# Get this from your Shopify Partners dashboard under App credentials
SHOPIFY_API_SECRET=your_api_secret_here

# Required: OAuth Scopes
# Comma-separated list of scopes your app needs
# Example: "read_products,write_products,read_orders,write_orders"
SCOPES=read_products,write_products,read_orders,write_orders

# Required: App URL
# Your app's public URL (e.g., https://your-app.herokuapp.com)
# During development, Shopify CLI sets this automatically
SHOPIFY_APP_URL=https://your-app-domain.com

# Optional: Custom Shop Domain
# Only needed if you're using a custom shop domain
# SHOP_CUSTOM_DOMAIN=your-custom-domain.com

# Optional: Node Environment
# Set to "production" when deploying to production
# Defaults to "development" if not set
NODE_ENV=development

# Optional: Database URL (Production)
# For production, use PostgreSQL or MySQL connection string
# Example PostgreSQL: postgresql://user:password@localhost:5432/size_chart
# Example MySQL: mysql://user:password@localhost:3306/size_chart
# If not set, defaults to SQLite (file:./prisma/dev.sqlite) for development
# DATABASE_URL=postgresql://user:password@localhost:5432/size_chart
